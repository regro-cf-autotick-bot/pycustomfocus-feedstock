{% set name = "pycustomfocus" %}
{% set version = "3.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/PyCustomFocus-{{ version }}.tar.gz
  sha256: be872eb31748799597d9c89327a917e30c9a2b96fecdb7c9200b4cc4f76f598e

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - numpy
    - scipy
    - qdarkstyle
    - config
    - tqdm
    - matplotlib-base
    - pyqt
    - qtpy
    - pyqtgraph
    - configparser

test:
  imports:
    - PyFocus
    - PyFocus.sim
    - PyFocus.VP_functions
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/fcaprile/PyFocus
  summary: Full vectorial calculation of focused electromagnetic fields moduled by a custom phase mask
  license: MIT
  license_file: LICENCE.txt

extra:
  recipe-maintainers:
    - jaimergp
    - goanpeca
